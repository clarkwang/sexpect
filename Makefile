
all:
	@echo  Please use cmake to build:
	@echo
	@echo " $$ mkdir build"
	@echo " $$ cd build"
	@echo " $$ cmake .."
	@echo " $$ make"
	@echo " $$ make install # will install to '/usr/local/' by default"
